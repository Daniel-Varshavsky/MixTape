package com.example.mixtape.model

enum class ShareMethod {
    DIRECT, // Shared directly to a specific user
    SHARE_CODE, // Shared via shareable code
    QR_CODE // Shared via QR code (future feature)
}
